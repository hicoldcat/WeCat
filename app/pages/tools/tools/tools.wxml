<view>
    <view wx:for="{{toolList}}" class="toolBox">
        <view class="imgbox">
            <image src="{{item.src}}"></image>
        </view>
        <view class="title">{{item.title}}</view>
    </view>
</view>